apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'application'

mainClassName = 'nim.assign'

repositories {
	mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.10'
}

jar {
  manifest {
    attributes("MainClass": "nim.assign")
  }
}
